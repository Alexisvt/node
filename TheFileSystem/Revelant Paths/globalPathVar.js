/*
Variables that provide insight as to where in the file system Node
is working
*/

// filename is the absolute path of the currently executing file
console.log("The file is " + __filename);

//__dirname is the absolute path to the directory containing the currently executing file
console.log("It's located in " + __dirname);
